pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                sh 'whoami'
                sh 'ls'
                echo 'Hello world!'
            }
        }
    }
}