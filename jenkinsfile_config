pipeline {
    agent any

    stages {
        stage('Config') {
            steps {
                sh 'cd ./terraform'
                dir('terraform') {
                    sh 'ls -alt'
                    sh 'terraform init'
                    sh 'terraform apply -auto-approve'
                }
                echo 'Hello world!'
            }
        }
    }
}