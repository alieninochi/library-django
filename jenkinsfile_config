pipeline {
    agent any

    stages {
        stage('Config') {
            steps {
                sh 'cd ./terraform'
                sh 'terraform init'
                sh 'terraform apply'
                echo 'Hello world!'
            }
        }
    }
}